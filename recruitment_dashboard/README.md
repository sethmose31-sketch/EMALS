# RecruitEase ATS (minimal)

Simple Flask dashboard that reads `data/candidates.json` and provides a table and basic charts.

Run locally:

```bash
cd recruitment_dashboard
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open http://localhost:5000
